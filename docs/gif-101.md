# GIF 101 — How It Works

A technical walkthrough of the codebase: what gif does, how it's structured, and the order of execution for a tool call.

---

## What it is, physically

Two Docker containers: a PostgreSQL 16 database and a Node.js MCP server. No message broker, no cache, no external services. Everything is in the database.

```
Client (AI model / test script)
    ↓  HTTP POST to :3100/mcp
MCP Server (mcp-server/src/index.ts)
    ↓  validates persona, validates the session handle, dispatches tool
PostgreSQL (gif schema)
    ↓  personas, sessions, audit_events, scope_violations
```

---

## The three concepts you need

**Persona** — a governance identity for an AI agent. Not a user, not a service account. It carries a declared `purpose` (required — non-nullable, schema constraint), a `scope_definition` (JSONB: what tools and sources are permitted), temporal bounds (`valid_from`, `valid_until`), and an optional parent persona for delegation chains. Personas are created by human administrators before any AI action occurs.

**Session** — a governance envelope with an explicit handle (v0.2, GIF-019/020). The AI calls `session_start` once, receives a `gif_session_id` (UUID), and passes it as an argument on every governed tool call. The dispatcher validates the handle before the tool runs — ownership, closed, expired (`validateSessionHandle`). Sessions end when the caller closes them (`session_close`) or when the deployment-wide TTL lapses (`GIF_SESSION_TTL_SECONDS`, default 86400 seconds) — they are **not** auto-closed per call. One session groups many tool calls in the audit trail.

**Audit event** — the immutable record. Every governed call that reaches governance evaluation produces a row in `audit_events`: who (persona), what session, which tool, what outcome (`allowed` / `denied` / `deferred` / `error`), sources touched, purpose declared. Rows are hash-chained at insert time by a database trigger (SHA-256 over a canonical byte form). `gif_app` (the runtime role) has INSERT-only on this table — no UPDATE, no DELETE at the database permission level.

---

## Directory structure

```
gif/
├── docker-compose.yml               # Postgres + MCP server
├── .env.example                     # Required env vars template
├── package.json                     # Exports gif-enforcement module
│
├── mcp-server/
│   ├── src/
│   │   ├── index.ts                 # Entry point: HTTP server, enforcement dispatch
│   │   ├── enforcement.ts           # createEnforcement(pool) — the public API
│   │   ├── enforcement_instance.ts  # Module-level gif instance
│   │   ├── db.ts                    # PostgreSQL pool singleton (gif_app role)
│   │   ├── persona.ts               # Persona validation used by the dispatcher
│   │   ├── session.ts               # Session-handle + audit helpers used by the dispatcher
│   │   ├── audit/
│   │   │   └── verify-core.ts       # Pure verifier core: canonicalization, hash recompute, chain checks (no DB)
│   │   ├── cli/
│   │   │   ├── issue_identity_token.ts # HMAC identity-token issuer (persona provisioning)
│   │   │   └── verify_audit_chain.ts   # Hash-chain verifier CLI — DB shell around src/audit/verify-core.ts
│   │   └── tools/
│   │       ├── registry.ts          # TOOL_REGISTRY: Map<name, ToolHandler>
│   │       ├── types.ts             # ToolHandler interface
│   │       ├── arg-guards.ts        # Dispatcher-generic required-argument guard
│   │       ├── sql-identifier.ts    # SQL identifier validation/escaping for the db tools
│   │       ├── persona_validate.ts
│   │       ├── persona_create.ts
│   │       ├── persona_revoke.ts
│   │       ├── session_start.ts     # Mints the gif_session_id governance handle
│   │       ├── session_close.ts     # Caller-driven session closure
│   │       ├── db_read.ts           # Reference adopter tool — template for your own handlers
│   │       └── db_write.ts          # Reference adopter tool — template for your own handlers
│   ├── conformance/                 # GIF-022 conformance scenarios c1–c6 +
│   │   └── audit-record-contract/   #   SEP-3004 audit-record vectors (npm run vectors)
│   ├── test_setup.mjs               # Creates seed personas for tests
│   ├── test_mcp.mjs                 # E2E: connect, call db_read, verify response
│   ├── test_input_guards.mjs        # Required-arg + value-constraint guards (92 cases)
│   ├── test_sql_injection.mjs       # SQL-identifier injection hardening
│   ├── test_audit_trail.mjs         # Audit trail completeness, RLS enforcement
│   ├── test_delegation.mjs          # Delegation chain validation
│   ├── test_hash_chain.mjs          # Cryptographic hash chain integrity
│   ├── test_chain_verifier.mjs      # Verifier CLI against chained + tampered rows
│   ├── test_identity_binding.mjs    # User-to-persona identity binding
│   ├── test_read_log.mjs            # Audit log read access
│   ├── test_retention.mjs           # Audit retention policy enforcement
│   ├── test_combination_policies.mjs # Combination policy evaluator (8 cases)
│   ├── test_session_lifecycle.mjs   # session_start / session_close semantics
│   └── test_session_dispatch.mjs    # Dispatcher session-handle validation paths
│
├── schema/
│   ├── 000_bootstrap.sql            # Roles: gif_admin (owner), gif_app (runtime)
│   ├── 001_gif_core.sql             # Core tables: personas, sessions, audit_events, ...
│   ├── 002–012_*.sql                # Incremental migrations
│   ├── 013_session_v2_semantics.sql # v0.2 session semantics (GIF-020)
│   ├── 014_audit_canonical_json.sql # Audit canonical form (gif-audit/1)
│   ├── 015_audit_canonical_json_v2.sql # Audit canonical form (gif-audit/2)
│   └── 016_audit_chain_concurrency.sql # Serialized chain writes (lock table)
│
├── ops/docker/init-db.sh            # Docker entrypoint: bootstrap, passwords, migrations
│
└── docs/                            # Public-facing documentation
    ├── runbooks/contributor/
    ├── runbooks/adopter/
    ├── gif-product-overview.md
    ├── gif-plain-language-guide.md
    ├── adopter-invocation-context.md  # Required and recommended invocation context fields
    └── architecture-diagrams.md
```

---

## Order of execution for a single tool call

`index.ts` is the chokepoint. Every tool call follows this sequence (v0.2 —
GIF-019/020/022):

```
1. Client sends CallTool { name, arguments: { persona_id, gif_session_id, ... } }
   (gif_session_id was minted by a prior session_start call)

2. Argument-shape checks: missing arguments object or persona_id
   → protocol InvalidParams error. No audit event — the call is rejected
     before governance evaluation begins (GIF-022 §C2.7)

3. TOOL_REGISTRY.get(name) → ToolHandler  (unknown tool → MethodNotFound)

4. validatePersona(persona_id)
   → SELECT from gif.personas
   → checks: exists, status = 'active', valid_from ≤ now ≤ valid_until,
             governance_review_status ∈ { 'auto_approved', 'approved' }
     (explicit allowlist — ADR-017 stub semantics; 'auto_approved' is the
      schema default every persona_create produces)
   → failure: return error result, nothing logged

5. Required-argument guard (tools/arg-guards.ts, dispatcher-generic):
   any argument listed in the tool's inputSchema.required that is absent
   → protocol InvalidParams, no audit event (same C2.7 boundary as step 2)

6. If skipSession = true (persona_validate, session_start, session_close):
   → execute directly — no session validation. session_start mints its own
     handle; session_close operates on the caller-supplied handle;
     persona_validate emits no audit event.

7. Governed tools: validateSessionHandle(persona_id, gif_session_id, TTL)
   → single PK SELECT on gif.sessions
   → rejects: not found / persona mismatch / closed / expired
     (closure precedence: closed wins over expired; fail-closed on DB error)
   → rejection: best-effort audit event (session_rejected_closed or
     session_expired, outcome 'denied'), then error result returned

8. toolHandler.execute(args, persona, sessionId)
   → tool runs its logic with the validated session id
   → may invoke checkCombinationPolicies (aggregation risk; adopter-invoked
     per GIF-012 framework boundary)
   → may log scope violations internally
   → returns ToolResult { content, isError }

9. logAuditEvent({ personaId, sessionId, eventType, toolName, outcome, ... })
   → INSERT INTO audit_events (hash-chained by trigger)
   → outcome: 'allowed', or 'error' if the tool threw or returned isError
   → never throws — audit failure cannot mask tool response
   → runs in a finally block — executes whether the tool succeeded or threw

10. Return ToolResult to client. The session is NOT closed — closure is
    caller-driven (session_close) or TTL-driven (GIF-020).
```

---

## The tool registry

`tools/registry.ts` exports a `Map<toolName, ToolHandler>`. The enforcement engine has zero knowledge of which tools exist or what they do — it only knows the `ToolHandler` interface. Adding a tool means adding it to the map.

**One handler per action.** Each discrete operation the AI is allowed to perform is a separate handler — `app_search`, `app_create_entry`, `app_delete` are three handlers, not one handler with an `action` parameter. This is not convention — it is what makes Persona scope enforcement meaningful at the action level. If two operations share a handler, you cannot scope a Persona to one without the other.

**Secrets.** Handler code accesses application credentials via `process.env`. API keys, tokens, and any other secrets required by a handler belong in environment variables — not in the GIF database. For development, add them to `.env` alongside the GIF credentials. For production, they come from the adopter's secret management infrastructure (AWS Secrets Manager, Vault, Kubernetes Secrets, etc.) — injected as environment variables when the container starts. GIF does not manage application credentials; that is the adopter's operational concern.

**`db_read.ts` and `db_write.ts` are reference adopter tools**, not GIF framework tools. They exist to demonstrate the handler pattern and can be read as templates. Adopter implementations follow the same shape with their own application logic in place of the gif table queries.

Current tools (7 active):

| Tool | Purpose | skipSession |
|------|---------|------------|
| `persona_validate` | Check if a persona is valid | yes — no audit event |
| `session_start` | Mint a governance session handle (`gif_session_id`) | yes — mints its own; emits a `session_start` audit event |
| `session_close` | Close a session (caller-driven closure, GIF-020) | yes — operates on the supplied handle; emits `session_close` |
| `persona_create` | Create a new persona (requires `identity_token` + `created_by`) | no — first-class audit event |
| `persona_revoke` | Revoke or suspend a persona | no — terminates active sessions |
| `db_read` | Reference adopter tool — query gif tables (allowlisted); demonstrates the handler pattern | no |
| `db_write` | Reference adopter tool — write to gif tables; demonstrates the handler pattern | no |

Every governed tool (`skipSession` absent/false) requires `gif_session_id` in
its `inputSchema` — the dispatcher pre-validates the handle before the handler
runs; handlers never re-implement session validation (GIF-019, GIF-022 §C2).

---

## The enforcement package

`enforcement.ts` exports `createEnforcement(pool)` — a factory that returns enforcement functions bound to the injected PostgreSQL pool. This is what adopter servers import. They inject their own `pg.Pool`; gif does not own the connection.

Key functions:

- **`validatePersona(personaId)`** — checks existence, active status, temporal bounds, governance-review allowlist
- **`validateSessionHandle({ personaId, gifSessionId, ttlSeconds })`** — the GIF-020 dispatcher primitive and the single source of truth for session-handle validity. Adopters call it between persona validation and tool execution; they must not re-implement closure precedence (closed > expired) or TTL handling. Fail-closed on DB error
- **`createSession(personaId, invocationContext)`** / **`closeSession(sessionId)`** — session row INSERT / `ended_at` UPDATE; used by the `session_start` / `session_close` handlers (not per-call by the dispatcher)
- **`logAuditEvent(params)`** — INSERT into audit_events; never throws
- **`logScopeViolation(params)`** — INSERT into scope_violations; never throws
- **`verifyIdentityBinding({ identityToken })`** — HMAC identity-token verify-and-consume at `persona_create`
- **`logAuditRead(params)`** — records `db_read` access to audit-class tables; never throws
- **`checkCombinationPolicies(sessionId, personaId, incomingSourceRefs)`** — pre-execution aggregation risk check; evaluates active policies with first-match resolution (v0.1; exhaustive evaluation is a v0.2 trajectory item); fails closed on DB error. Caller-declared `incomingSourceRefs` is trusted by this v0.1 evaluator — runtime declaration validation is a v0.2 trajectory item

---

## Database schema at a glance

| Table | Purpose | gif_app grant |
|-------|---------|---------------|
| `personas` | Governance identities | SELECT, INSERT — `admin_read` permission required to query via MCP tools |
| `sessions` | Per-invocation envelopes | SELECT, INSERT, UPDATE |
| `audit_events` | Immutable action log | INSERT only |
| `scope_violations` | Out-of-scope attempt records | INSERT only |
| `delegation_chain` | Parent→child persona relationships | SELECT, INSERT |
| `revocation_log` | Persona state change history | SELECT, INSERT |
| `tool_registry` | Capability inventory | SELECT |
| `combination_policies` | Aggregation risk rules | SELECT |
| `user_persona_assignments` | External user → persona bindings | SELECT, INSERT, UPDATE |
| `schema_migrations` | Applied migration tracking | SELECT, INSERT |

All tables live in the `gif` schema. `gif_admin` owns all objects and runs migrations. `gif_app` is the runtime role — it cannot DELETE or UPDATE audit tables.

---

## Adopter accountability obligations

GIF records what happened, under which persona, and when — as a structural guarantee. What GIF cannot capture automatically is the human and system context behind the call: which human account the AI is running under, which product session triggered it, which environment it ran in. This is the adopter's responsibility.

**`responsible_human_id` must be present in `invocation_context` on every invocation.** Without it, the audit trail has a runtime accountability gap — GIF can name the provisioning human (structurally recorded at persona creation) but not the human whose account the AI was operating under at runtime.

For service account or background job invocations where no user is active, pass a namespaced system identifier. An absent field is not traceable; a namespaced string is.

The full contract — required fields, recommended fields, and patterns for service accounts, sub-agents, and product sessions — is in [`docs/adopter-invocation-context.md`](adopter-invocation-context.md).

**Note on `persona_id` as a bearer token:** GIF validates that a persona is active and in scope but does not structurally verify that the calling process is the authorized holder of the `persona_id`. This is a deliberate, documented constraint (GIF-014). The mitigation: `personas` is gated behind `admin_read` (not enumerable), every persona has a named provisioner, and the audit trail is append-only. Adopters fulfilling the `responsible_human_id` obligation complete the accountability chain.

---

## How to get output

**Start the stack:**
```bash
cp .env.example .env        # fill in passwords
docker compose up -d --build
docker compose logs postgres # watch init — look for "GIF Init Complete"
```

**Smoke test:**
```bash
curl -s http://localhost:3100/health
# → {"status":"ok","service":"gif-mcp-server"}
```

**Run the full integration suite (14 test files + 6 conformance scenarios, real PostgreSQL — no functional mocks; one error-injection mock in `test_combination_policies` Test 8 to exercise the fail-closed path):**
```bash
cd mcp-server && npm install && npm run build && cd ..
./scripts/test-local.sh
```

`scripts/test-local.sh` is the canonical runner: it sources `.env`, sets the
PG/HMAC env vars the suite expects, and constructs `MCP_BASE_URL` from `PORT`
so the tests hit *your* compose stack — not another gif server that happens
to be listening on the default port.

Tests connect directly to the postgres container, create personas, call tools over HTTP to the MCP server, and verify the audit trail was written correctly. No test framework — plain Node.js `.mjs` files with manual assertions.

**Reset to a clean state:**
```bash
docker compose down -v      # drops postgres_data volume
docker compose up -d        # fresh init, all 16 migrations reapply
```
