# GIF-009 — Streamable HTTP Transport

**Status:** Accepted  
**Date:** 2026-04-04

## Decision

The MCP server uses the Streamable HTTP transport (single `/mcp` endpoint, per-session transport instances). The deprecated SSE transport is not supported.

See `ADR-002-streamable-http-transport.md` for full rationale and migration record.
